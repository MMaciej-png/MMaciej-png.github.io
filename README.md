A small program to learn indonesian using a reactive flashcard system using html, css and js.

to wipe all data, In Inspect Element Run the following :

(() => { console.warn("wiping ALL local data");

localStorage.clear(); sessionStorage.clear();

location.reload(); })();

this will wipe all data, nothing will be saved.